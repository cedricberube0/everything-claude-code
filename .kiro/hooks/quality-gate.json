{
    "version": "v1",
    "hooks": [
        {
            "name": "quality-gate",
            "description": "Run a full quality gate check (build, type check, lint, tests) after a spec task completes.",
            "trigger": "PostTaskExec",
            "action": {
                "type": "command",
                "command": "bash .kiro/scripts/quality-gate.sh"
            },
            "enabled": true
        }
    ]
}